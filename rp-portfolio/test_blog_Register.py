# from selenium import webdriver
# from selenium.webdriver.common.keys import Keys
# import pytest
# import time

# def test_Register_User(): #please try once as it will effect the password
	# # It would go to admin page to test the the login function on username and password
	 # driver = webdriver.Chrome()
	 # driver.get("http://localhost:8000/admin/login/?next=/admin/")
	 # elem = driver.find_element_by_name("username")
	 # elem.send_keys("AmsyarMazri")

	 # elem = driver.find_element_by_name("password")
	 # elem.send_keys("16S29t96")
	 # elem.send_keys(Keys.RETURN) #press enter key
	
	 # driver.get("http://localhost:8000/admin/blog/post/add/")
	 # elem = driver.find_element_by_name("title")
	 # elem.send_keys("Introduction")

	 # elem = driver.find_element_by_name("body")
	 # elem.send_keys("What is Selenium")
	 # elem.send_keys(Keys.RETURN) #press enter key
	 
	 # elem = driver.find_element_by_name("categories")
	 # elem.send_keys("Category object (1)")
	 # elem.send_keys(Keys.RETURN)
	 
	 
	 # elem = driver.find_element_by_name("_save").click()
	 # assert driver.current_url == 'http://localhost:8000/admin/'

	 # time.sleep(3)
	 # driver.close()












 